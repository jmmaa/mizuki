{
  "name": "mizuki",
  "version": "1.1.6dev",
  "description": "a tiny javascript library that helps you create paginated behavior for your app components.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "npx del-cli --force './dist/*' && tsc && esbuild --bundle dist/index.js --minify --outfile=dist/index.js --allow-overwrite --format=esm",
    "dev": "npx tsc --watch",
    "test": "jest",
    "start": "npx ts-node src/index.ts"
  },
  "author": "jmmaa",
  "repository": {
    "url": "https://github.com/jmmaa/mizuki"
  },
  "license": "MIT",
  "type": "module",
  "private": false,
  "devDependencies": {
    "@types/jest": "^29.2.0",
    "@types/node": "^18.11.7",
    "del-cli": "^5.0.0",
    "esbuild": "^0.15.12",
    "jest": "^29.2.2",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
